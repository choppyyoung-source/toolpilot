import type { Metadata } from "next";
export const metadata: Metadata = { title: "Diff Checker — Compare Text Online", description: "Free online diff checker. Compare two texts and see differences highlighted line by line. Perfect for code, documents, and configs.", keywords: ["diff checker", "text compare", "diff tool online", "compare text", "code diff"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
