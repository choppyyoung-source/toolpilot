import type { Metadata } from "next";
export const metadata: Metadata = { title: "JSON to CSV Converter — Convert JSON Online", description: "Free online JSON to CSV converter. Convert JSON arrays to CSV format instantly. Download as CSV file.", keywords: ["json to csv", "convert json to csv", "json csv converter", "json to csv online", "json to spreadsheet"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
