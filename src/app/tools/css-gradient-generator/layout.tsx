import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Gradient Generator — Create Gradients Online", description: "Free CSS gradient generator. Create beautiful linear and radial gradients with live preview and copy CSS code instantly.", keywords: ["css gradient generator", "gradient maker", "css gradient", "linear gradient generator", "radial gradient"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
