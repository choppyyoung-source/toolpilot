import type { Metadata } from "next";
export const metadata: Metadata = { title: "Regex Tester — Test Regular Expressions Online", description: "Free online regex tester. Test regular expressions with real-time matching, highlighting, and match details. Supports JavaScript regex.", keywords: ["regex tester", "regular expression tester", "regex online", "regex checker", "regex debugger"] };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
